colors = ['red', 'green', 'blue']

# Adds the specifid list elements (or any iterable) to the end of the current list
axample_list = ['orange', 'violet', 'grey']
colors.extend(axample_list)
print(colors)

example_tupla = ('yellow', 'black', 'white')
colors.extend(example_tupla)
print(colors)
